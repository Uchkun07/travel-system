server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/travel_system?useSSL=false
    username: root
    password: 102030
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379
      password: ""
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 64
          max-idle: 16
          min-idle: 4
          max-wait: 2000ms
  mail:
    host: smtp.qq.com
    port: 587
    username: 2208844091@qq.com
    password: tczyvqbqpplyeahe
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          socketFactory:
            class: javax.net.ssl.SSLSocketFactory
    default-encoding: UTF-8
  servlet:
    multipart:
      enabled: true
      max-file-size: 5MB
      max-request-size: 5MB
  web:
    resources:
      static-locations: classpath:/static/
      add-mappings: true
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

# 文件上传目录配置（Linux 服务器）
file:
  upload:
    base-dir: /opt/travel-system/uploads
    avatar-dir: /opt/travel-system/uploads/avatars

# DeepSeek AI 配置
deepseek:
  api:
    url: https://api.deepseek.com/chat/completions
    key: sk-30589b3e516044b98603f19985934166
    model: deepseek-chat
    timeout: 60 # 请求超时时间(秒)

# JWT配置
jwt:
  secret: travel-system-secret-key-for-jwt-token-generation-and-validation-2024
  expiration: 604800000 # 7天(毫秒)
  expiration-short: 86400000 # 1天(毫秒)

# 高德地图 API 配置
amap:
  key: ${AMAP_KEY:7af4638cb0fd5d313df83870b542eba6}
  geocode-url: https://restapi.amap.com/v3/geocode/geo
  driving-url: https://restapi.amap.com/v5/direction/driving
  transit-url: https://restapi.amap.com/v5/direction/transit/integrated
  timeout: 8000 # HTTP 超时时间(ms)

# 路线规划算法配置（模拟退火参数可调）
route:
  algorithm:
    # 初始温度（越高探索空间越大）
    initial-temp: 10000.0
    # 冷却系数（0.9~0.999）
    cooling-rate: 0.995
    # 最小温度（终止条件）
    min-temp: 1.0
    # 每温度迭代次数
    iterations-per-temp: 100
    # 经济偏好：时间/费用权重（0=只看费用，1=只看时间）
    pref-economy-time-weight: 0.2
    pref-comfort-time-weight: 0.8
    pref-moderate-time-weight: 0.5

# 个性化推荐算法配置
recommend:
  algorithm:
    behavior-window-days: 30
    behavior-switch-threshold: 10
    # 冷启动：70%偏好 + 30%热门
    cold-preference-weight: 0.7
    cold-hot-weight: 0.3
    # 稳定期：90%画像 + 10%热门
    mature-profile-weight: 0.9
    mature-hot-weight: 0.1
    # 画像拆分：35%显式 + 65%隐式
    profile-explicit-weight: 0.35
    profile-implicit-weight: 0.65
    # 隐式行为拆分：45%点击 + 55%停留
    behavior-click-weight: 0.45
    behavior-stay-weight: 0.55
    rec-version: content-java-v2

# MyBatis-Plus 配置
mybatis-plus:
  configuration:
    # 生产环境默认关闭SQL标准输出日志，必要时通过环境变量开启
    log-impl: ${MYBATIS_LOG_IMPL:org.apache.ibatis.logging.nologging.NoLoggingImpl}
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# 后端高并发性能参数
performance:
  recommend-timeout-ms: 1200
  route-plan-timeout-ms: 10000
  cache:
    attraction-list-ttl-sec: 90
    attraction-detail-ttl-sec: 300
    top-attraction-ttl-sec: 120
    recommend-ttl-sec: 45
    route-plan-ttl-sec: 180
    null-value-ttl-sec: 30
    ttl-jitter-sec: 30
  executor:
    general-core-pool-size: 8
    general-max-pool-size: 32
    route-core-pool-size: 4
    route-max-pool-size: 16
    queue-capacity: 1024
    keep-alive-seconds: 60
